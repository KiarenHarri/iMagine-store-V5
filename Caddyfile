# Already set to your domain. Only change this if you decide to use a
# different one. Caddy automatically creates free HTTPS certificates the first
# time someone visits — your domain's DNS must already point at this server.
imaginestore.co.za, www.imaginestore.co.za {
	reverse_proxy frontend:80
}
